<% if @obj.errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
    <h4><%= pluralize(@obj.errors.count, "error") %> prohibited this <%= @obj.class.name
     %> from being saved:</h4>
    <ul>
    <% @obj.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
  <br />
<% end %>
