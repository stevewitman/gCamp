<div>
  <div class="page-header">
    <div class="pull-right text-muted">
      (showing first 500)
    </div>
    <h1>Stories for (gCamp)</h1>
  </div>

  <table class="table">
    <thead>
      <tr>
        <th>Story</th>
        <th>Estimate</th>
        <th>Labels</th>
        <th>Current State</th>
      </tr>
    </thead>
    <tbody>
      <% if current_user.admin %>
        <% @projects.each do |project| %>
          <tr>
            <td><%= link_to %></td>
            <td><%= %></td>
            <td><%= %></td>
            <td><%= %></td>
          </tr>
        <% end %>
      <% else %>
        <% @current_memberships.each do |m| %>
          <tr>
            <td><%= link_to %></td>
            <td><%= %></td>
            <td><%= %></td>
            <td><%= %></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
